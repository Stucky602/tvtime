// Placeholder shell. Component 1 (repo scaffold) stops here on purpose --
// the actual tab UI is components 11-13 per ARCHITECTURE_v1.0.md §11, and
// they depend on the schema (2), RPCs (6), and deck build module (9)
// landing first. See README.md for the build order.
export default function App() {
  return (
    <div style={{ display: 'grid', placeItems: 'center', minHeight: '100vh' }}>
      <p>Streaming Swipe -- scaffold only, no screens yet.</p>
    </div>
  )
}
